<tbody>
{% for data in models %}
    <tr>
        {% for column in columns %}
            <td>
            {% set index = loop.parent.loop.index0 %}
            {% set myfilter = column.twigFilter %}
            {{ column.render(data, index)| myfilter }}
            {# {{ column.render(data, index) }} #}
            {# {{ column.render(data, index) }} #}
            </td>
        {% endfor %}
    </tr>
{% else %}
    <tr>
        <td colspan="{{ columns.length }}">no records found</td>
    </tr>
{% endfor %}
</tbody>
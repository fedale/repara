security:
    # https://symfony.com/doc/current/security/experimental_authenticators.html
    enable_authenticator_manager: true
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'bcrypt'
        # App\Entity\User\User:
        #     algorithm: bcrypt
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User\User
                property: username
        
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        # api:
        #     pattern: ^/authentication
        #     stateless: true
        #     provider: app_user_provider
        #     json_login:
        #         check_path: /authentication
        #         success_handler: lexik_jwt_authentication.handler.authentication_success
        #         failure_handler: lexik_jwt_authentication.handler.authentication_failure       

        # refresh:
        #     pattern: ^/token/refresh
        #     stateless: true

        main:
            # pattern: ^/
            #stateless: true
            provider: app_user_provider
            json_login:
                check_path: /authentication
                success_handler: lexik_jwt_authentication.handler.authentication_success
                failure_handler: lexik_jwt_authentication.handler.authentication_failure
            jwt: ~
        
        # main:
        #     pattern: ^/admin
        #     lazy: true
        #     provider: app_user_provider
            # form_login:
            #    login_path: admin_login
            #    check_path: admin_login
            #guard:
            #    authenticators:
            #        - App\Security\BackendAuthenticator
            


            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Il controllo d'accesso per-rotta è gestito da fedale/access-control-bundle
    # (listener su kernel.request, regole nella tabella access_control).
    # Vedi config/packages/fedale_access_control.yaml.
    access_control:
        # - { path: ^/authentication, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        # - { path: ^/token/refresh, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        # - { path: ^/api/docs, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        # - { path: ^/, roles: IS_AUTHENTICATED_FULLY }

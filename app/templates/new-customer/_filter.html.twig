<thead id="" class="filter" data="">
    <tr class="">
        {{ form_start(form) }}
        {% for column in columns %}
        <th class="">
            {{ form_widget(form.task, {'attr': {'class': 'task_field'}}) }}
            <!-- <input type="text" name="{{column.label}}" placeholder="" style="background-color: #404040; border: 0"> -->
        </th>
        {% endfor %}
        <input type="submit">
    {{ form_end(form) }}
    </tr>
</thead>
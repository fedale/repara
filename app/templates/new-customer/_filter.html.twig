<thead id="" class="filter" data="">
    <tr class="">
        {{ form_start(form, {'method': 'GET'}) }}
        {% for column in columns %}
        <th class="">
            {{ form_widget(form[column.label], {'attr': {'class': 'foo'}}) }}
        </th>
        {% endfor %}
        {{ form_widget(form.save) }}
        {{ form_end(form) }}
    </form>
    </tr>
</thead>
<thead {{ gridview.headerAttr|options}}>
    <tr>
        {% for column in columns %}
        <th>
            {{ column.renderHeader(column.label)|raw }}
        </th>
        {% endfor %}
    </tr>
</thead>
<thead {{ gridview.filterAttr|options }}>
    <tr>
        {% for column in columns %}
        <th>
            {% if form[column.attribute] is defined %}
                {{ form_widget(form[column.attribute], {
                    'attr': {'data-action': 'input->gridview-filter#input'}
                }) }}
            {% endif %}
        </th>
        {% endfor %}
        <th>{{ form_widget(form.save) }}</th>
    </tr>
</thead>

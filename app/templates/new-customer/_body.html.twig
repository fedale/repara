<tbody>
{% for data in models %}
    <tr>
        {% for column in columns %}
            <td>
                {{ dump(column.twigFilter) }}
            {% set index = loop.parent.loop.index0 %}
            {{ column.render(data, index)|apply_filter(column.twigFilter) }}
            {# {{ column.render(data, index) }} #}
            {# {{ column.render(data, index) }} #}
            </td>
        {% endfor %}
    </tr>
{% else %}
    <tr>
        <td colspan="14">no records found</td>
    </tr>
{% endfor %}
</tbody>